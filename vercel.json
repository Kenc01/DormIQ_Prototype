{
  "buildCommand": "pnpm --filter @workspace/dorm-app run build && mkdir -p dist && cp -a artifacts/dorm-app/dist/public/. dist/",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "BASE_PATH": "/"
  }
}
